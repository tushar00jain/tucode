# tucode

Run: `npm install`, `npm start -- [folder]`, or `mac/launch.sh [folder]`. Check relevant changes with `npm run typecheck`, `npm run typecheck:mac-web`, `npm run test:unit`, and `npm run test:host`; see [test commands](test/e2e/README.md) and [Mac commands](mac/README.md).
Reuse VS Code's models and services; keep Mac adapters independent of `src/tui/`. Avoid reformatting vendored code. Keep docs brief and track unfinished work in [docs/TODO.md](docs/TODO.md).
