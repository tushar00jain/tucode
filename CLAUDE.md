# tscode

Run: `npm install`, `npm run tauri:dev`; check changes with `npm run typecheck`, `npm run test:unit`, and `npm run test:rust` as relevant.
Reuse upstream code and service boundaries; avoid reformatting vendored files. Keep docs brief. See [README.md](README.md) for other commands.
