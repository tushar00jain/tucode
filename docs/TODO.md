# TODO

- Fix terminal sticky scroll requesting the unavailable ligatures addon, which
  produces a module-loading error when the overlay opens.
- Wire accessibility sound assets into the build: playing a signal currently
  fails when resolving its media URL because the file root is unset.
